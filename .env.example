# .env.example — copy to .env and fill in real values. Never commit .env to Git.

# Django
DJANGO_SECRET_KEY=your-secret-key-here-generate-with-python-c-"import-secrets;print(secrets.token_hex(50))"
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# PostgreSQL — leave blank to use SQLite (dev only)
# DATABASE_URL=postgresql://user:password@localhost:5432/event_management_db
